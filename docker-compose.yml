services:
  backend:
    image: registry.fauzi.studentdumbways.my.id/fe-dumbmerch:production
    container_name: be-production
    restart: always
    ports:
      - "5001:5000"
    env_file:
      - .env
